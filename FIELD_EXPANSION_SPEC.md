# Z-SAGA フィールド拡張仕様

このパックは、現在のゲーム一式を置き換えるものではありません。2枚の追加背景と、既存コードへ差分統合するための設定だけを収録しています。

## フィールド進行

| 順番 | フィールド | 解放条件 | 通常敵HP | 敵攻撃 | G | ボス前討伐数 | 推奨仲間Lv |
|---|---|---|---:|---:|---:|---:|---:|
| 1 | アズライト大陸 | 最初から | ×1.00 | ×1.00 | ×1.00 | 10 | 3 |
| 2 | 闇の森・クローゼット深部 | アズライトのボス撃破 | ×1.65 | ×1.35 | ×1.45 | 12 | 5 |
| 3 | 洗濯槽深層・ランドリーアビス | 闇の森のボス撃破 | ×2.60 | ×1.85 | ×2.00 | 16 | 8 |

ボス撃破フラグは `GameState.worldProgress.defeatedBosses` に保存し、`localStorage` にも保存してください。再読み込み後も解放済みフィールドを維持します。

## ボス

- アズライト大陸：百着を呑む呪衣装棚 / S / HP580
- 闇の森：無限袖のクローゼットロード / SS / HP1350
- 洗濯槽深層：大渦布竜スピン・レヴィアタン / SSS / HP2400

各フィールドで指定数の通常敵を倒すと、背景上部のボスゲートが光り、接触でボス戦へ入ります。未達成時は「あと○体倒すとボスの気配が強まる」と表示します。

## Gによる仲間育成

既存の「同じ仲間を引いた時の重複強化」は残したまま、ステータスメニューへ `仕立工房` を追加します。

- レベルアップ費用：`120 + 現在Lv × 100 G`
- 選択した仲間だけLv+1
- 既存の `getPartyPowerMultiplier()` がLvを参照するため、攻撃力へそのまま反映
- 重複強化の `duplicates / attackBonus / skillLevel` は変更しない
- 同じキャラクターを複数追加しない既存仕様も維持

## 背景

- `assets/fields/field_azurlight_meadow.png`：800×600
- `assets/fields/field_wardrobe_gloomwood.png`：800×600
- `assets/fields/field_laundry_abyss.png`：800×600

Canvasへ原寸で描画し、画像補間を無効化してください。キャラクター、当たり判定、UIは背景とは別レイヤーに維持します。

3枚とも論理解像度100×75相当を800×600へニアレスト拡大し、1ドット約8×8px・約24色の荒い見下ろし型へ統一済みです。連続した道・通路・周回路は3ステージすべてから削除し、全面を自由に歩ける地面として構成しています。48pxキャラクターが背景へ埋もれないようにしています。闇の森の服は外周の3着だけで、木・地面・光る植物が主役です。

## フィールドアニメーション

`field-expansion.js` の `drawAnimatedField(ctx, image, fieldId, time)` を、既存の `drawMap` と同じ描画ループから呼びます。

- 草原：外周の葉と茂みが8px刻みで揺れ、2つの池へ角張った波紋が広がる
- 闇の森：外周の木と3着の服を8px刻みで揺らし、ボタン蛍を周期違いで点滅
- 洗濯槽：18個の角張った泡を8pxグリッドで異なる速度で上昇させ、薄い水中光を横へ流す
- アニメーションは背景レイヤーだけ。キャラクター座標、当たり判定、戦闘判定には影響させない
- `Math.random()` を毎フレーム使わず、同じ軌道を再現してちらつきを防ぐ

## ステージ別BGM

既存のWeb Audioシステムを残したまま、フィールド曲だけ3種類に分けます。既存曲のメロディーは流用せず、Z-SAGAのオリジナル曲にしてください。

- アズライト大陸 `field-meadow`：104 BPM。明るい笛風の主旋律、軽い弦の分散和音、冒険の始まり
- 闇の森 `field-wardrobe`：74 BPM。短調、低いドローン、木音、ボタンが鳴るようなベル。静かで不穏
- 洗濯槽深層 `field-laundry`：92 BPM。浮遊感のある三角波、水滴のような高音、揺れる低音。泡が上がる感覚

フィールド移動時は `changeField(nextFieldId)` が `GameState.currentFieldId` と背景画像の更新を完了した直後に、必ず `switchFieldMusic(nextFieldId, audioBridge)` を1回呼びます。現在の曲を約600msでフェードアウトし、新しい曲をフェードインします。同じ曲IDなら再起動しません。

戦闘・ボス・召喚へ入る時は従来どおり専用BGMを優先します。終了後は固定で草原曲へ戻さず、必ず `GameState.currentFieldId` を読み、そのステージの曲へ戻します。BGMが二重再生されないように、フィールド曲・戦闘曲・召喚曲のうち同時に鳴るのは1系統だけにします。BGM以外のゲーム処理やタイミングは変更しません。

## 統合時に変更してはいけないもの

- 召喚、ランク別演出、BGM、重複統合
- 現在の横視点戦闘と敵の反撃
- 服の出品、評価通知
- キャラクター画像、スプライトシートのセル配置
- 既存の敵HP障壁、スキル貫通、Perfect Fit
- 既存ファイル名と画像パス

`field-expansion.js` は設定値のみです。現在のコードを丸ごと置き換えず、必要な設定と処理だけを既存の `GameState`、フィールド描画、エンカウント処理、ステータスメニューへ統合してください。
